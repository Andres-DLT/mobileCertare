import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'redboyconsulting.marketplace',
  appName: 'marketplace',
  webDir: 'dist/marketplace/browser'
};

export default config;
