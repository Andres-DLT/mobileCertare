import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certare.app',
  appName: 'Certare',
  webDir: 'dist/phoneDevelopers/browser'
};

export default config;
